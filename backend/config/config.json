{
  "development": {
    "use_env_variable": "DATABASE_URL",
    "dialect": "postgres",
    "pool": {
      "max": 20,
      "min": 1,
      "acquire": 60000,
      "idle": 60000
    },
    "dialectOptions": {
      "connectTimeout": 60000,
      "statement_timeout": 60000,
      "query_timeout": 60000
    }
  },
  "test": {
    "use_env_variable": "DATABASE_URL",
    "dialect": "postgres"
  },
  "production": {
    "use_env_variable": "DATABASE_URL",
    "dialect": "postgres",
    "pool": {
      "max": 20,
      "min": 1,
      "acquire": 60000,
      "idle": 60000
    },
    "dialectOptions": {
      "ssl": {
        "require": false,
        "rejectUnauthorized": false
      },
      "connectTimeout": 60000,
      "statement_timeout": 60000,
      "query_timeout": 60000
    }
  }
}